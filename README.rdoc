== Eventsvis API

Eventsvis is a news visualizer. This app is the API for eventsvis-web, where news are actually visualized.

== Instalation

1. Install rvm and ruby 1.9.3
2. Install MySQL
3. Create a gemset
        rvm gemset create eventsvis-api
4. Install project gems
        bundle install